#ifndef ACTIVATION_FUNCTIONS_H
#define ACTIVATION_FUNCTIONS_H

float ReLu(float);
float d_ReLu(float);

float Sigmoid(float);
float d_Sigmoid(float);

float lineal(float);
float d_lineal(float);

#endif