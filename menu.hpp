#ifndef MENU_HPP
#define MENU_HPP

#include <array>
#include <vector>
#include <tuple>
#include <string>
#include <iostream>

using namespace std;

bool options_check(int argc, vector<string> args);
void options (int argc, vector<string> args);

#endif